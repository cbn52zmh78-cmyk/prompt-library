# David Linguist — orchestration pointer

Canonical system prompt for dead-language research and Grok training:

`DAVID/prompts/david_linguist_system.md`

Quick invoke: *"Use the DAVID protocol for [language]. Corpus-first. Tag uncertainty."*

Tools:
- `python tools/david_status.py`
- `python DAVID/scripts/grok_training_pack_builder.py --language <slug>`